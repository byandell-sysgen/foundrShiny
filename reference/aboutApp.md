# About App

About App

## Usage

``` r
aboutApp(id)

aboutServer(id, helppath = NULL, entry = shiny::reactive(TRUE))

aboutOutput(id)
```

## Arguments

- id:

  identifier for shiny reactive

- helppath:

  path to help markdown

- entry:

  entry logical flag

## Value

reactive server
