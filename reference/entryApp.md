# Entry App for password

Entry App for password

## Usage

``` r
entryApp()

entryServer(id, customSettings = NULL)

entryInput(id)

entryUI(id)

entryOutput(id)
```

## Arguments

- id:

  identifier for shiny reactive

- customSettings:

  list of custom settings

## Value

reactive server
